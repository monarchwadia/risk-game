import { Player } from "../../types";
import { Component } from "./component.type";

type Props = {
  players: Player[]
}
const ScoreComponent: Component<Props> = ({players}) => {
  return;
}

export default ScoreComponent;